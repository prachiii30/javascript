"use strict";    //treat all js code as newer version
// alert("hellow") wont work in the node envi only in browser

// important docs=> tc39 and mdn

 let name="Prachi";
 let age=89;
 let isLoggedIn=true;

//  number -> 2 to the power 53
// bigint ->  
// string ->" "
// boolean -> either true or false
// null -> stand alone value  let state = null;
// undefined -> let state; if we try to console.log(state) we get o/p as undefined
//              variable declared but not assigned a value
// symbol  -> whenever we tlk abt uniqness
// object
 
console.log(typeof "prachi");  //string
console.log(typeof null); //object
console.log(typeof undefined);  //undefined